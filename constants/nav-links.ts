const NAV_LINKS = (locale: string) => [
  { path: `/${locale}`, name: 'home' },
  { path: `/${locale}/about`, name: 'about' },
  { path: `/${locale}/contact-me`, name: 'contact-me' },
];

export default NAV_LINKS;
